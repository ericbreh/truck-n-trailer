# MPC

This folder contains the Model Predictive Control implementation for reverse parking a truck-and-trailer system. It uses Pyomo with the IPOPT solver to find optimal control inputs.

## To run

```sh
# Run from repo root
python -m mpc.simulation
```
