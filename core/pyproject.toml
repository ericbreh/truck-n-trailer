[project]
name = "core"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "vision",
    "control",
    "gui"
]

[tool.uv.workspace]
members = ["vision", "control", "gui"]

[tool.uv.sources]
vision = { workspace = true }
control = { workspace = true }
gui = { workspace = true }
